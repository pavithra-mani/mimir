# FastAPI and web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
pydantic-settings>=2.0

# Auth & database (used by api/auth, models/database)
sqlalchemy>=2.0
bcrypt
PyJWT>=2.0

# Optional summarization API
google-generativeai>=0.8.0

# Core ML/AI dependencies (macOS compatible)
numpy==1.26.4
scipy==1.13.1
scikit-learn==1.4.2
transformers==4.40.2
tokenizers==0.19.1
torch>=2.0.0
torchaudio>=2.0.0

# Audio processing (macOS compatible)
faster-whisper>=1.1.1
stable-ts

# Use pre-built wheels for macOS to avoid compilation issues
# Note: whisperx and pyannote.audio may need manual installation on macOS
# Install separately after main requirements

# Video processing
opencv-python-headless
Pillow
ffmpeg-python
imagehash>=4.3.1

# Text processing
nltk
pandas
tqdm
spacy
# English pipeline used by knowledge graph + retrieval (required at runtime):
#   python -m spacy download en_core_web_sm
datasets
accelerate

# Vector store and embeddings
sentence-transformers==2.7.0
faiss-cpu
networkx

# Utilities
pyyaml
requests
aiofiles

# For production deployment
redis>=4.0.0
celery>=5.0.0

# macOS specific dependencies
# Install these separately after main requirements:
# pip install --no-cache-dir --no-build-isolation pyannote.audio==3.1.1
# pip install --no-cache-dir --no-build-isolation whisperx
